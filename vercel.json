{
  "version": 2,
  "cleanUrls": true,
  "buildCommand": "npm run vercel-build",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
